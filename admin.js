function generateRandomCode() {    return Math.random().toString(36).substring(2, 8); // توليد كود عشوائي}function populateCodes() {    const codeTable = document.getElementById('code-table');    codeTable.innerHTML = ''; // مسح الأكواد القديمة        for (let i = 0; i < 10; i++) {        const newRow = codeTable.insertRow();        const newCell = newRow.insertCell();        const code = generateRandomCode();        codes[i] = code; // حفظ الكود في القائمة        newCell.textContent = code;    }}populateCodes(); // تعبئة الأكواد عند تحميل الصفحة// تحديث الأكواد كل 10 دقائق (600000 مللي ثانية)setInterval(populateCodes, 600000);