let codes = []; // قائمة الأكواد العشوائيةlet usedCodes = new Set(); // الأكواد التي تم استخدامهاfunction verifyCode() {    const userCode = document.getElementById('user-code').value;        // تحقق إذا كان الكود صحيح ولم يُستخدم    if (codes.includes(userCode) && !usedCodes.has(userCode)) {        usedCodes.add(userCode); // أضف الكود إلى القائمة المستخدمة        startTimer(); // ابدأ العداد        document.getElementById('code-form').style.display = 'none'; // إخفاء النموذج        document.getElementById('timer').style.display = 'block'; // إظهار العداد    } else {        alert('Invalid or used code');    }}function startTimer() {    let timeRemaining = 10 * 60; // 10 دقائق    const interval = setInterval(() => {        let minutes = Math.floor(timeRemaining / 60);        let seconds = timeRemaining % 60;        document.getElementById('time-remaining').textContent = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;                if (timeRemaining <= 0) {            clearInterval(interval);            alert('Time is up!');        }        timeRemaining--;    }, 1000);}document.getElementById('code-form').style.display = 'block';